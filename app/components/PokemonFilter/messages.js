/*
 * PokemonFilter Messages
 *
 * This contains all the text for the PokemonFilter component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PokemonFilter';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PokemonFilter component!',
  },
});
